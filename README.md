# 📜 Announcement Board REST API (Дошка оголошень з JWT-автентифікацією)

Це сучасний RESTful API для повнофункціонального сервісу розміщення та перегляду оголошень. Проєкт повністю переведений на архітектуру чистого API, який спілкується з клієнтом виключно через формат JSON.

У цій версії додано повноцінну систему **JWT-автентифікації з Refresh-токенами (HttpOnly Cookies)** та механізмом **Token Rotation**, а також захист маршрутів оголошень на основі прав власності (**Ownership**).

## 📋 Функціональні можливості API

### 🔐 Маршрути Автентифікації (`/auth`)
1. **POST /auth/register** — Реєстрація нового користувача. Хешує пароль за допомогою `bcrypt`, автоматично видає пару токенів (`access` + `refresh`), записує `refresh` токен у базу та встановлює його в безпечну `HttpOnly` cookie.
2. **POST /auth/login** — Вхід у систему. Перевіряє логін/пароль, генерує нову пару токенів, замінює старий refresh-токен у базі (захист від перевикористання).
3. **POST /auth/refresh** — Оновлення `access` токена. Приймає `refresh` токен з cookie або тіла запиту, реалізує **Token Rotation** (старий токен видаляється, новий записується).
4. **POST /auth/logout** — Вихід із системи. Видаляє `refresh` токен із бази даних та очищує клієнтські cookies.
5. **GET /auth/me** — Отримання профілю поточного авторизованого користувача (повертає дані без пароля).

### 📢 Маршрути Оголошень (`/announcements`)
1. **GET /announcements** — Отримання списку оголошень (*Публічний*).
   * **Пошук:** Нечутливий до регістру кирилиці пошук підрядка в полі `title`.
   * **Сортування:** Параметр `sort` (`newest` або `oldest`).
   * **Пагінація:** По 10 записів на сторінку (`page`). Обчислення метаданих виконується паралельно через `Promise.all()`.
2. **GET /announcements/:id** — Отримання повної інформації про одне оголошення за його ID (*Публічний*).
3. **POST /announcements** — Створення нового оголошення (*Захищений*). Автоматично прив'язує оголошення до `userId` поточного користувача.
4. **PATCH /announcements/:id** — Часткове оновлення оголошення (*Захищений, Ownership*). Редагувати можна **лише власні** оголошення. Порожній запит `{}` заборонений.
5. **DELETE /announcements/:id** — Видалення оголошення (*Захищений, Ownership*). Видалити можна **лише власні** оголошення. Повертає статус `204 No Content`.

---

## 🛠 Технологічний стек

* **Runtime:** Node.js (ES-Modules, `"type": "module"`)
* **Web Framework:** Express 5
* **Database & ORM:** SQLite + Prisma ORM
* **Security:** JWT (`jsonwebtoken`), `bcrypt` для хешування паролів
* **Validation:** celebrate / Joi
* **API Documentation:** Swagger UI (`swagger-ui-express`, `swagger-jsdoc`)

---

## ⚙️ Інструкція зі встановлення та запуску

### 1. Встановлення залежностей
Клонуйте репозиторій та встановіть усі необхідні пакети:

```bash
git clone https://github.com/ErnestSosnovskyi/AnnouncementBoard.git
cd AnnouncementBoard
npm install
```

---

### 2. Налаштування змінних оточення

Створіть або оновіть файл .env у корені проєкту та додайте секрети для підпису токенів:

```bash
DATABASE_URL="file:./dev.db"
JWT_SECRET="your_super_secret_access_key_2026"
JWT_REFRESH_SECRET="your_even_more_secure_refresh_key_2026"
```

---

### 3. Налаштування бази даних та міграція

Для оновлення структури бази даних (створення таблиць `User`, `RefreshToken` та зв'язків з `Announcement`) виконайте міграцію:

```bash
npm run prisma:migrate
```

*(Примітка: Якщо в базі були старі тестові оголошення без автора, Prisma запропонує скинути базу або створити дефолтного користувача. Для чистокровного запуску використовуйте `npm run prisma:reset`).*

---

### 4. Запуск сервера

Запустіть сервер у режимі розробки:

```bash
npm run dev
```

Після успішного запуску сервер працюватиме за адресою: http://localhost:3000.

---

## 📑 Тестування та документація API

### 🌐 Інтерактивна документація Swagger

Вся документація до ендпоінтів (включаючи нові маршрути авторизації) задокументована за допомогою JSDoc-коментарів і відображається в інтерактивному інтерфейсі:

👉 http://localhost:3000/api-docs

Для тестування захищених маршрутів спочатку виконайте `/auth/login`, скопіюйте `accessToken` та вставте його у вікно Authorize (формат: `Bearer <ваш_токен>`).

---

### ⚡ Файл запитів `requests.http`

У корені проєкту знаходиться оновлений файл `requests.http`. За допомогою розширення REST Client для VS Code ви можете протестувати:

* Усі сценарії авторизації (реєстрація, логін, рефреш, логаут, профілі).
* Спроби доступу до захищених маршрутів оголошень без токена (повертає `401 Unauthorized`).
* Симуляцію перевірки Ownership (спроба Користувача 2 змінити або видалити оголошення Користувача 1 повертає `403 Forbidden`).

---

## 📂 Структура проєкту

```Plaintext
hw4/
├── prisma/
│   ├── schema.prisma           # Оновлена схема БД (моделі User, RefreshToken, Announcement)
│   ├── client.js               # Експорт ініціалізованого Prisma Client
│   └── migrations/             # Історія міграцій бази даних
├── src/
│   ├── controllers/
│   │   ├── announcements.controller.js  # Логіка оголошень з перевіркою Ownership
│   │   └── auth.controller.js           # Логіка JWT, bcrypt та Token Rotation
│   ├── routes/
│   │   ├── announcements.routes.js      # Публічні та захищені маршрути оголошень
│   │   └── auth.routes.js               # Маршрути модуля автентифікації
│   ├── validators/
│   │   ├── announcements.validators.js  # Схеми валідації оголошень
│   │   └── auth.validators.js           # Валідація Joi для реєстрації та входу (min/max обмеження)
│   └── middleware/
│       └── auth.middleware.js           # Перевірка Bearer JWT токена та запис у req.user
├── app.js                      # Точка входу, реєстрація cookieParser, Swagger, Error Handlers
├── requests.http               # Повний набір HTTP-запитів для демонстрації та тестування
├── .env                        # Секрети та налаштування оточення
└── package.json                # Скрипти запуску та залежності
```

## 🛡 Правила валідації та безпеки

**Реєстрація користувача:**
* `username` — рядок, обов'язковий, від 3 до 30 символів (унікальний у базі).
* `password` — рядок, обов'язковий, мінімум 6 символів. Зберігається в БД виключно у вигляді хешу `bcrypt`.
* `name` — рядок, обов'язковий, мінімум 2 символи.

При помилках валідації `celebrate` повертає статус `400 Bad Request`. При спробі дублювання `username` сервер повертає `409 Conflict`. У разі спроби редагування або видалення оголошення іншого користувача — повертається `403 Forbidden` з повідомленням `Access denied`.